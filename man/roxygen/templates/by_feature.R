#' @param by_feature Logical. If \code{TRUE}, the geometric operation is applied to each
#' geometry independently. If \code{FALSE} (default), the geometric operation is applied
#' to the data as a whole
